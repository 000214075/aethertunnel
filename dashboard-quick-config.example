# AetherTunnel Web 管理面板 - 快速配置示例
# 适合快速上手的精简配置

[dashboard]
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🌐 基础配置
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 是否启用 Web 管理面板
enabled = true

# 监听端口
port = 7500

# 绑定地址
# 127.0.0.1 - 仅本地访问（推荐，更安全）
# 0.0.0.0 - 所有接口（可以从外网访问，需额外安全措施）
bind_addr = "127.0.0.1"

# 访问路径（如使用反向代理，可设置为 /aethertunnel）
base_path = ""

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🔐 认证配置
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[dashboard.auth]
# 是否启用认证（强烈建议启用！）
enabled = true

# 认证方式：basic（基础认证）, jwt（令牌认证）
mode = "basic"

# 默认管理员账号
username = "admin"

# 默认管理员密码（生产环境请修改！）
password = "admin123"

# Session 超时时间（秒）
# 3600 = 1小时，86400 = 24小时
session_timeout = 3600

# Session 最大并发数（同一账号最多同时登录数）
max_concurrent_sessions = 5

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🔒 安全配置
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[dashboard.auth.rate_limit]
# 是否启用速率限制（防止暴力破解）
enabled = true

# 每分钟最大登录尝试次数
max_attempts_per_minute = 5

# 每小时最大登录尝试次数
max_attempts_per_hour = 20

# IP 白名单（仅允许这些 IP 访问）
[dashboard.auth.ip_whitelist]
enabled = false
allowed_ips = [
    "127.0.0.1",
    "192.168.1.0/24"
]

# IP 黑名单（禁止这些 IP 访问）
[dashboard.auth.ip_blacklist]
enabled = false
blocked_ips = [
    "1.1.1.1",
    "2.2.2.2"
]

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🎨 界面配置
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[dashboard.ui.theme]
# 默认主题：light（浅色）, dark（深色）, auto（跟随系统）
default_theme = "dark"

# 是否允许用户切换主题
allow_theme_switch = true

[dashboard.ui.branding]
# 应用名称
app_name = "AetherTunnel"

# 页面标题
page_title = "AetherTunnel 管理面板"

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 📡 WebSocket 实时更新
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[dashboard.websocket]
# 是否启用 WebSocket（实时显示数据）
enabled = true

# 心跳间隔（秒）
ping_interval = 30

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🔌 API 配置
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[dashboard.api]
# 是否启用 REST API
enabled = true

# API 速率限制
[dashboard.api.rate_limit]
enabled = true
requests_per_minute = 60

# API 密钥认证
[dashboard.api.auth.api_key]
# 是否启用 API 密钥
enabled = false

# API 密钥列表
api_keys = [
    "sk_prod_xxxxx",
    "sk_dev_xxxxx"
]

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🔒 TLS/SSL 配置（可选）
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[dashboard.tls]
# 是否启用 HTTPS
enabled = false

# 证书文件路径
cert_file = "/path/to/dashboard.crt"

# 私钥文件路径
key_file = "/path/to/dashboard.key"

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 📝 日志配置
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[dashboard.logging]
# 日志级别：debug, info, warn, error
level = "info"

# 审计日志（记录登录、配置修改等操作）
[dashboard.logging.audit]
enabled = true
audit_log_file = "/var/log/aethertunnel/audit.log"

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🚀 快速配置场景
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 场景 1：本地开发环境
# ─────────────────────────────────────────────────────
# [dashboard]
# enabled = true
# bind_addr = "127.0.0.1"
# port = 7500
#
# [dashboard.auth]
# enabled = true
# username = "admin"
# password = "admin123"

# 场景 2：生产环境（推荐）
# ─────────────────────────────────────────────────────
# [dashboard]
# enabled = true
# bind_addr = "127.0.0.1"  # 只监听本地，通过反向代理访问
# port = 7500
#
# [dashboard.auth]
# enabled = true
# username = "admin"
# password = "your-strong-password-here"
# session_timeout = 3600
#
# [dashboard.auth.rate_limit]
# enabled = true
# max_attempts_per_minute = 5
#
# [dashboard.tls]
# enabled = true  # 启用 HTTPS
# cert_file = "/etc/ssl/certs/dashboard.crt"
# key_file = "/etc/ssl/private/dashboard.key"
#
# [dashboard.logging.audit]
# enabled = true

# 场景 3：多用户团队协作
# ─────────────────────────────────────────────────────
# [dashboard]
# enabled = true
# bind_addr = "127.0.0.1"
# port = 7500
#
# [dashboard.auth]
# enabled = true
# mode = "basic"
# max_concurrent_sessions = 10
#
# [dashboard.auth.rate_limit]
# enabled = true
#
# [dashboard.api.auth.api_key]
# enabled = true  # 启用 API 密钥供自动化脚本使用
# api_keys = ["team-key-1", "team-key-2"]

# 场景 4：仅 API 访问（无 Web UI）
# ─────────────────────────────────────────────────────
# [dashboard]
# enabled = false  # 禁用 Web UI
#
# [dashboard.api]
# enabled = true  # 只启用 API
#
# [dashboard.api.auth.api_key]
# enabled = true
# api_keys = ["production-api-key"]

# ============================================================================
# 💡 配置提示
# ============================================================================
#
# 1. 安全建议：
#    ✅ 生产环境务必启用认证
#    ✅ 使用强密码（至少 12 位，包含大小写字母、数字、特殊字符）
#    ✅ 启用速率限制防止暴力破解
#    ✅ 使用 HTTPS（TLS）
#    ✅ 定期查看审计日志
#
# 2. 性能优化：
#    ✅ 根据需求调整 session_timeout
#    ✅ 合理设置速率限制
#    ✅ 启用日志轮转
#
# 3. 使用场景：
#    ✅ 本地开发：127.0.0.1 + 简单密码
#    ✅ 生产环境：127.0.0.1 + 强密码 + HTTPS + 速率限制
#    ✅ 团队协作：多用户 + API 密钥
#    ✅ 自动化：仅 API + API 密钥
#
# 4. 访问方式：
#    ✅ 直接访问：http://localhost:7500
#    ✅ 反向代理：Nginx / Caddy / Traefik
#    ✅ SSH 隧道：ssh -L 7500:localhost:7500 user@server
#
# ============================================================================
