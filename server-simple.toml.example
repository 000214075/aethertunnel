# ============================================================================
# AetherTunnel 服务端配置 - 小白快速上手版
# 只需要填写少量必填项，高级功能按需开启
# ============================================================================

# ----------------------------------------------------------------------------
# 🔰 基础配置（必填）
# ----------------------------------------------------------------------------
[server]
# 绑定地址（一般不需要改）
bind_addr = "0.0.0.0"

# 监听端口（默认 7000，避免与常用端口冲突）
bind_port = 7000

# 认证令牌（必填！建议使用强随机字符串，如：a3f9e2c1b8d4e6f5a7）
# 这是服务端和客户端之间的"密码"，两边必须一致才能连接
auth_token = "change-this-to-a-secure-random-token"

# 虚拟主机端口（可选，如果你要用域名访问服务）
# vhost_http_port = 80
# vhost_https_port = 443

# ----------------------------------------------------------------------------
# 🔐 安全配置（可选，但强烈推荐）
# ----------------------------------------------------------------------------
# 启用 TLS 加密（生产环境强烈建议开启）
# 如果开启，需要提供证书文件路径（见下方证书配置）
[tls]
enabled = false  # true = 启用，false = 不启用

# 证书和私钥（开启 TLS 时必填）
# cert_file = "/path/to/server.crt"
# key_file = "/path/to/server.key"

# 是否要求客户端证书（双向认证，更安全但配置更复杂）
# client_auth = false

# ----------------------------------------------------------------------------
# 🛡️ 访问控制（可选）
# ----------------------------------------------------------------------------
[security]
# 最大连接数（防止被滥用，默认 10000）
max_connections = 10000

# 心跳超时（秒，默认 90，客户端多久没消息就断开）
heartbeat_timeout = 90

# 连接超时（秒，默认 10）
connection_timeout = 10

# 启用审计日志（记录登录、操作等，方便排查问题）
enable_audit_log = false

# 审计日志文件路径
# audit_log_file = "/var/log/aethertunnel/audit.log"

# 速率限制（每秒最大连接数，防止暴力破解，默认 100）
rate_limit = 100

# 失败次数达到阈值后自动封禁 IP（默认 5 次）
max_failed_attempts = 5

# 封禁时长（秒，默认 300 = 5 分钟）
block_duration = "5m"

# ----------------------------------------------------------------------------
# 📊 Web 管理面板（可选）
# ----------------------------------------------------------------------------
[dashboard]
# 是否启用 Web 管理界面（默认启用）
enabled = true

# 监听端口（默认 7500，避免与上面端口冲突）
port = 7500

# 绑定地址（默认只监听本地，更安全）
# 如果要从外网访问，改为 "0.0.0.0"，但建议配合反向代理
bind_addr = "127.0.0.1"

# 登录用户名（默认 admin，建议修改）
username = "admin"

# 登录密码（默认 admin，生产环境务必修改！）
password = "admin"

# ----------------------------------------------------------------------------
# 🌐 高级功能（按需开启，小白可以忽略）
# ----------------------------------------------------------------------------
# 传输优化（提高性能）
# [transport]
# tcp_mux = true  # TCP 多路复用（默认开启）
# max_pool_count = 5  # 连接池大小（默认 5）

# 日志配置
# [logging]
# level = "info"  # 日志级别：debug, info, warn, error
# log_file = "/var/log/aethertunnel/server.log"

# 数据库存储（用于持久化状态，可选）
# [database]
# type = "none"  # none, mysql, postgresql, redis
# host = "localhost"
# port = 3306
# username = ""
# password = ""

# 负载均衡（多台服务器场景，可选）
# [load_balancer]
# enabled = false
# algorithm = "least_conn"

# 监控和指标（可选）
# [monitoring]
# prometheus_enabled = false
# prometheus_port = 9090

# 告警通知（可选）
# [notification]
# enabled = false
# [notification.email]
# smtp_server = "smtp.gmail.com:587"
# smtp_username = ""
# smtp_password = ""

# 备份（可选）
# [backup]
# enabled = false
# interval = 24  # 小时
# backup_dir = "./backups"

# ----------------------------------------------------------------------------
# 🔥 颠覆性功能（按需开启，需要更多配置）
# ----------------------------------------------------------------------------
# AI 智能路由（自动选择最佳路径，实验性功能）
# [ai_routing]
# enabled = false

# WebRTC P2P（点对点直连，无需中继，实验性功能）
# [webrtc]
# enabled = false

# 去中心化 DHT 网络（无需中心服务器，实验性功能）
# [dht]
# enabled = false

# 量子抗性加密（对抗未来量子计算机，实验性功能）
# [pqc]
# enabled = false

# 更多高级功能配置...
# 参见：server-toml-innovative-addon.example

# ============================================================================
# 💡 小白指南
# ============================================================================
#
# 1️⃣ 第一次使用？
#    - 只需要修改 [server] 里的 auth_token
#    - 其他都用默认值就可以！
#
# 2️⃣ 生产环境部署？
#    - 开启 [tls] 配置（获取证书文件）
#    - 修改 [dashboard] 里的 username 和 password
#    - 建议开启 [security] 的所有选项
#
# 3️⃣ 需要监控和管理？
#    - [dashboard] 已默认开启，访问 http://localhost:7500
#    - 默认用户名：admin，密码：admin
#    - 修改密码更安全！
#
# 4️⃣ 遇到问题？
#    - 查看 [security] 的日志文件
#    - 启用 [logging] 的 debug 级别查看详细信息
#
# 5️⃣ 想试试高级功能？
#    - 查看 server-toml-innovative-addon.example
#    - 文档：docs/INNOVATIVE_FEATURES.md
#    - 功能非常丰富，但对新手不友好
#
# ============================================================================
